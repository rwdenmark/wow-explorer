services:
  postgres:
    image: postgres:16
    container_name: wow-explorer-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-wow}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wow}
      POSTGRES_DB: ${POSTGRES_DB:-wow_explorer}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - wow_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-wow} -d ${POSTGRES_DB:-wow_explorer}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  wow_pg_data:
